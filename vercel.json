{
  "buildCommand": "npm install && npx tailwindcss -i ./assets/css/main.css -o ./static/css/main.css --minify && hugo --minify",
  "outputDirectory": "public",
  "framework": null,
  "env": {
    "HUGO_VERSION": "0.162.0"
  }
}
